---
- name: создание сети
  community.docker.docker_network:
    name: "{{ networkName }}"
    driver: overlay

- name: Выкладка сервисов
  include: "../services/{{ item }}/services.yml"
  vars:
    - name: "{{ item }}"
  loop: "{{ services }}"