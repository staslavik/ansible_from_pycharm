---
- name: создание сети
  community.docker.docker_network:
    name: "{{ networkName }}"
    driver: overlay

- name: Выкладка сервисов
  include: "../services/{{ item.name }}/services.yml"
  vars:
    - name: "{{ item.name }}"
    - version: "{{ item.version }}"
  loop: "{{ services }}"